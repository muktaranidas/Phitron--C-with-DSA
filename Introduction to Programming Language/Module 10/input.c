#include<stdio.h>
int main ()
{
    // power of string
    // input 
    char a[6];
    scanf("%s" , a);
    // int sz = sizeof(a) / sizeof(char);
    // printf("%d" , sz);
    printf("%s " , a);
    return 0;
}